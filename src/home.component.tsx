import React from "react";
import styles from "./home.scss";

const Home: React.FC = () => {
  return (
    <section className={styles.section}>
      <div> This is the home for HIE </div>
    </section>
  );
};

export default Home;
